export interface ForceInfo {
  id: string;
  name: string;
  motto: string;
  description: string;
  icon: string;
  quote: string;
}

export interface SupportMessage {
  id: number;
  name: string;
  message: string;
  location: string;
}